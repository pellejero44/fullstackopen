const CREATED = {
  type: '@notification/CREATED',
};

const DELETED = {
  type: '@notification/DELETED',
};


const notifiationActions = {
  CREATED,
  DELETED, 
}

export default notifiationActions;